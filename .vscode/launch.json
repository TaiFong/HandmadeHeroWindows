{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch",
            "type": "node",
            "request": "attach",
            "program":"${workspaceFolder}/HandmadeHeroWindows",
            "args":[],
            "cwd": "${workspaceFolder}",
            "console": "integratedTerminal"
        }
    ]
}